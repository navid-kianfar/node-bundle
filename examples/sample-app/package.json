{
  "name": "sample-app",
  "version": "1.0.0",
  "description": "Tiny TypeScript app to validate the node-bundle pipeline.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "dayjs": "^1.11.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
